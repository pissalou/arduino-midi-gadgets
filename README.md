# arduino-midi-gadgets

Collection of MIDI-capable gadgets built with Arduino and ATTiny85 microcontrollers.

```bash
alias compile="arduino-cli compile -b ATTinyCore:avr:attinyx5micr"
compile sketch.ino
```